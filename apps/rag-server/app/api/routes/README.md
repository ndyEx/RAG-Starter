# 🕹 API Routes (파이썬 컨트롤러 계층)

이 폴더는 외부(프론트엔드)로부터 들어오는 **HTTP 요청을 가장 먼저 받는 진입점(Controller)** 들의 모음입니다.

완벽하게 **Spring의 `@RestController`** 와 역할이 같습니다.

- 클라이언트가 검색어(종목코드)를 던지면, 
- 여기서 그 요청을 받아 데이터(DTO)가 올바른지 검사하고 
- 진짜 비즈니스 로직을 처리하는 서비스 계층(`core/`)으로 책임을 넘깁니다.
